let Auth = require("../util/API");

module.exports = function (app) {

    app.post("/api/add_user", (res, req) => {
        // let user = req.body;
        // console.log(user);
        // let exists = Auth.userExists(user.email);
        // res.json(JSON.stringify({exists: exists}));
    })

}